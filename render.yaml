services:
  - type: web
    name: happy-provision-store
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: MONGODB_URI
        sync: false
      - key: DATABASE_URL
        sync: false
